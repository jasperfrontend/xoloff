---
paths:
  - 'resources/js/**'
---

# Js

## Tailwind: body copy colour and pointer cursors
Body copy uses text-foreground, not text-muted-foreground. Reserve muted for genuinely secondary chrome, not for readable content.

Every clickable element gets cursor-pointer: buttons, links, rows that navigate, icon triggers.
